<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Base" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/blueis</item>
        <item name="colorPrimaryVariant">@color/bluestrong</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/blueis</item>
        <item name="colorSecondaryVariant">@color/cian</item>
        <item name="colorOnSecondary">@color/bluee</item>
    </style>
    <style name="NextButtom" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:layout_height">55dp</item>
        <item name="android:layout_marginRight">25dp</item>
        <item name="android:layout_marginLeft">25dp</item>
        <item name="android:paddingLeft">60dp</item>
        <item name="android:paddingRight">60dp</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>

    <style name="Theme.Base" parent="Base.Theme.Base" />
</resources>